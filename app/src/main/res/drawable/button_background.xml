<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Transparent background -->
    <solid android:color="@android:color/transparent" />

    <!-- Orange border -->
    <stroke
        android:width="4dp"
        android:color="#ed5f1e" />

    <!-- Optional: Corner radius -->
    <corners android:radius="8dp"/>
</shape>